<svelte:head>
	<title>Swift SDK — Lazypock Docs</title>
</svelte:head>

<h1>Swift SDK</h1>

<p>
	Coming soon — a Swift client for Lazypock isn't released yet. When it lands, its pages will live
	under this section with the same structure as the
	<a href="/sdk/typescript">TypeScript SDK</a>.
</p>

<p>
	The SDK docs are sourced from each SDK's own repo (a <code>docs/</code> folder, synced at build time),
	so adding a new SDK only means pointing the sync script at it.
</p>
