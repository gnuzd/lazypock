{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://lazypock.gnuzd.dev/backup.schema.json",
	"title": "LazyPock backup / import payload",
	"description": "Full-database backup format accepted by POST /api/import and the Studio (Settings → Import / Backups). Generate this shape with GET /api/export. Documented at https://lazypock.gnuzd.dev/backup",
	"$comment": "The importer also accepts a bare array of collection objects, and PocketBase 23+ collections that use a top-level \"fields\" key with flat type options (see the docs page). This schema describes the canonical LazyPock export envelope.",
	"type": "object",
	"required": ["collections"],
	"properties": {
		"collections": {
			"type": "array",
			"description": "Every collection to create/update, with its records.",
			"items": { "$ref": "#/definitions/collection" }
		}
	},
	"definitions": {
		"collection": {
			"type": "object",
			"required": ["name", "type"],
			"if": { "properties": { "type": { "const": "view" } }, "required": ["type"] },
			"then": {},
			"else": { "anyOf": [{ "required": ["schema"] }, { "required": ["fields"] }] },
			"properties": {
				"id": {
					"type": "string",
					"description": "Existing collection id. Omit it and the collection is matched by name."
				},
				"name": {
					"type": "string",
					"pattern": "^[a-z][a-z0-9_]*$",
					"description": "Table name: starts with a letter, lowercase letters/numbers/underscores only."
				},
				"type": {
					"enum": ["base", "auth", "view"],
					"description": "base = plain records; auth = accounts with email + write-only password; view = SQL/view-driven read-only collection."
				},
				"schema": {
					"type": "array",
					"description": "Field definitions (required for base/auth, omitted for view). PocketBase exports call this \"fields\" instead — send one or the other, never both.",
					"items": { "$ref": "#/definitions/field" }
				},
				"fields": {
					"type": "array",
					"description": "PocketBase 23+ alias for \"schema\".",
					"items": { "$ref": "#/definitions/field" }
				},
				"rules": {
					"$ref": "#/definitions/rules"
				},
				"options": {
					"type": "object",
					"description": "Collection-level options. For view collections the query lives in view_query (SQL) or view_builder.",
					"properties": {
						"indexes": {
							"type": "array",
							"description": "Custom index expressions, e.g. \"UNIQUE email\", \"created_at DESC, id\".",
							"items": { "type": "string" }
						},
						"view_query": { "type": "string" },
						"view_builder": { "type": "object" }
					},
					"additionalProperties": true
				},
				"hooks": {
					"type": "object",
					"description": "Collection hook configuration. Usually {}."
				},
				"records": {
					"type": "array",
					"description": "Records to upsert by id. Omit for view collections (their rows are regenerated by the view).",
					"items": { "type": "object" }
				}
			},
			"additionalProperties": true
		},
		"field": {
			"type": "object",
			"required": ["name", "type"],
			"properties": {
				"name": {
					"type": "string",
					"pattern": "^[A-Za-z][A-Za-z0-9_]*$",
					"description": "Field name. Kept verbatim (case preserved); it is also the API key and DB column name."
				},
				"type": {
					"enum": [
						"text",
						"editor",
						"number",
						"bool",
						"email",
						"url",
						"date",
						"datetime",
						"autodate",
						"select",
						"multi_select",
						"file",
						"multi_file",
						"relation",
						"json",
						"geo",
						"password"
					]
				},
				"required": { "type": "boolean", "default": false },
				"unique": { "type": "boolean", "default": false },
				"indexed": { "type": "boolean", "default": false },
				"hidden": {
					"type": "boolean",
					"default": false,
					"description": "Never returned in API responses / not shown in the Studio."
				},
				"system": {
					"type": "boolean",
					"default": false,
					"description": "System-managed field. Fields with system=true are stripped on import (e.g. PocketBase's own \"id\" field) — do not include them."
				},
				"default": {
					"description": "Default value applied when the field is omitted on write."
				},
				"options": {
					"type": "object",
					"description": "Type-specific settings, e.g. { \"maxSelect\": 3 } for a multi select, { \"values\": [...] } for select, { \"collection\": \"other\" } for relation, { \"onCreate\": true } for autodate.",
					"additionalProperties": true
				},
				"sort_order": { "type": "integer", "description": "Display/column order." }
			},
			"additionalProperties": true
		},
		"rules": {
			"type": "object",
			"description": "PocketBase filter rules. null = superusers only; \"\" = public.",
			"properties": {
				"listRule": { "$ref": "#/definitions/nullableString" },
				"viewRule": { "$ref": "#/definitions/nullableString" },
				"createRule": { "$ref": "#/definitions/nullableString" },
				"updateRule": { "$ref": "#/definitions/nullableString" },
				"deleteRule": { "$ref": "#/definitions/nullableString" },
				"manageRule": { "$ref": "#/definitions/nullableString" }
			},
			"additionalProperties": true
		},
		"nullableString": {
			"type": ["string", "null"]
		}
	}
}
